# 本次上傳步驟

1. 在 Repository 的 Code 頁面選擇 `Add file` → `Upload files`。
2. 將本資料夾「裡面的所有內容」拖入上傳區，不要只拖外層資料夾。
3. 確認上傳清單中有 `app/page.tsx`、`components/Home.tsx`、`config/links.ts`、`data/tasks.ts`、`data/content.ts` 與 `tsconfig.json`。
4. Commit 訊息可填：`Update freshman tasks links and FAQ`。
5. 到 Actions 等待最新的 Pages workflow 顯示綠色勾勾。
6. 部署完成後，以 `Ctrl + F5` 強制重新整理網站。

本次不需要先刪除 GitHub 上殘留的 `src/`；新的 `tsconfig.json` 已排除該資料夾。
